===========================================================
模型改进总结报告
日期: 2026-07-13
基准分数: 0.024 → 当前分数: 0.031（提升+29%）
===========================================================

一、核心改进路径
-----------------------------------------------------------
1. 标签改造：绝对收益 → 相对超额收益排名（+0.006）
2. 特征筛选：去除38个高度冗余特征（+0.006）
3. 学习率调整：1e-5 → 2e-5（+0.001）

二、详细改动记录
-----------------------------------------------------------

1. 标签改造：绝对收益 → 相对超额收益排名
   文件: train.py (_build_label_and_clean函数)
   修改内容:
     - 计算个股5日绝对收益: abs_return = (open_t5 - open_t1) / open_t1
     - 计算当日市场平均收益: market_return = groupby('日期')['abs_return'].mean()
     - 计算超额收益: excess_return = abs_return - market_return
     - 将超额收益转化为百分位等级标签: label = rank(pct=True, ascending=False) * 100
   效果: 0.024 → 0.030

2. 特征筛选：相关性去重
   文件: utils.py (engineer_features_corr_filtered函数)
   修改内容:
     - 通过相关性分析发现38个高度冗余特征(相关系数>0.95)
     - 特征数从197个降至159个
     - 移除类别: 价格特征(收盘/最低/最高)、均线特征(sma/ema/boll_mid)、
       分位数特征(QTLU/QTLD)、极值特征(MAX/MIN)等
   效果: 0.030 → 0.030（稳定，信号密度提升）

3. 学习率调整
   文件: config.py
   修改内容:
     - learning_rate: 1e-5 → 2e-5
   理由: 特征减少后模型复杂度降低，需要更高学习率加速收敛
   效果: 0.030 → 0.031

三、辅助改进
-----------------------------------------------------------

4. 评估指标与评分脚本对齐
   文件: train.py (calculate_ranking_metrics函数)
   修改内容:
     - 添加top5_return指标: Top5股票等权平均收益率
     - targets用于评估(真实涨跌幅)，relevance用于训练(排名标签)

5. pairwise_loss修复：只保留排名方向
   文件: train.py (WeightedRankingLoss类)
   修改内容:
     - 使用torch.sign(true_diff)替代直接差值比较
     - 排名值是序数，只关心顺序不关心绝对差值

6. 滚动窗口验证框架
   文件: train.py (generate_rolling_windows, train_single_window, run_rolling_validation函数)
   修改内容:
     - 50个滚动窗口，每次向前滚动一周
     - 5天空窗期避免标签泄漏
     - 报告平均收益、正收益窗口比例、最大连续亏损等指标

四、当前配置
-----------------------------------------------------------
文件: config.py
- sequence_length: 60
- feature_num: 'corr_filtered'（159个特征）
- d_model: 256
- nhead: 4
- num_layers: 3
- batch_size: 4
- num_epochs: 50
- learning_rate: 2e-5
- dropout: 0.1
- pairwise_weight: 1
- top5_weight: 2.0

五、关键经验
-----------------------------------------------------------
1. 信号密度 > 特征数量：去除冗余特征提升了模型稳定性
2. 学习率与特征数量匹配：特征减少后需要提高学习率
3. 标签改造是最大收益来源：从绝对收益到相对超额收益排名
4. 滚动窗口验证确认分数稳定性：50个窗口平均分数0.030，说明模型没有过拟合
5. 标签格式（排名值vs百分位）影响有限：两种格式效果相同

六、下一步建议
-----------------------------------------------------------
1. 继续超参数搜索：
   - learning_rate: 尝试5e-5, 1e-4
   - dropout: 尝试0.15, 0.2
   - batch_size: 尝试8, 16

2. 后处理优化：
   - 根据预测分数分配权重（softmax归一化）
   - 添加单票权重上限（如≤30%）
   - 添加行业分散度约束

3. Ensemble融合：
   - 训练LightGBM LambdaRank作为辅助模型
   - hybrid_guard策略：树模型候选池 + Transformer高置信候选

4. 超参数两阶段搜索：
   - 单参数扫描取前3个较优值
   - 网格组合搜索
